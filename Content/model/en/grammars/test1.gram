#JSGF V1.0;

grammar check;

public <final_rule> = (<digit> <operation> <digit>) | (<action> <who>);

<operation> = ( add | subtract | divide | multiply);

<digit> = ( one | two | three | four | five | six | seven | eight | nine | zero );

<action> = ( change );

<who> = ( mode );